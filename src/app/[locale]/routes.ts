import {z} from 'zod';

export enum Route {
	Home = '/',
	LeaguesRankings = '/leagues-rankings',
	TypeChart = '/type-chart',
	FilterGenerator = '/filter-generator',
	PvpHelper = '/pvp-helper',
	IndividualRanking = '/individual-ranking',
	Settings = '/settings',
}

